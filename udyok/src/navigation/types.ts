/**
 * Shared navigation parameter types
 * These types are used across multiple stack navigators to ensure consistency
 */

export type SpaceDetailParam = {
    SpaceDetail: { spaceId: string };
};

export type BookingFlowParam = {
    BookingFlow: undefined;
};
